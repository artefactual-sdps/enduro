// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/artefactual-sdps/enduro/internal/persistence/ent/db/pkg"
	"github.com/artefactual-sdps/enduro/internal/persistence/ent/db/predicate"
	"github.com/artefactual-sdps/enduro/internal/persistence/ent/db/preservationaction"
	"github.com/artefactual-sdps/enduro/internal/persistence/ent/db/preservationtask"
)

// PreservationActionQuery is the builder for querying PreservationAction entities.
type PreservationActionQuery struct {
	config
	ctx         *QueryContext
	order       []preservationaction.OrderOption
	inters      []Interceptor
	predicates  []predicate.PreservationAction
	withPackage *PkgQuery
	withTasks   *PreservationTaskQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PreservationActionQuery builder.
func (paq *PreservationActionQuery) Where(ps ...predicate.PreservationAction) *PreservationActionQuery {
	paq.predicates = append(paq.predicates, ps...)
	return paq
}

// Limit the number of records to be returned by this query.
func (paq *PreservationActionQuery) Limit(limit int) *PreservationActionQuery {
	paq.ctx.Limit = &limit
	return paq
}

// Offset to start from.
func (paq *PreservationActionQuery) Offset(offset int) *PreservationActionQuery {
	paq.ctx.Offset = &offset
	return paq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (paq *PreservationActionQuery) Unique(unique bool) *PreservationActionQuery {
	paq.ctx.Unique = &unique
	return paq
}

// Order specifies how the records should be ordered.
func (paq *PreservationActionQuery) Order(o ...preservationaction.OrderOption) *PreservationActionQuery {
	paq.order = append(paq.order, o...)
	return paq
}

// QueryPackage chains the current query on the "package" edge.
func (paq *PreservationActionQuery) QueryPackage() *PkgQuery {
	query := (&PkgClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(preservationaction.Table, preservationaction.FieldID, selector),
			sqlgraph.To(pkg.Table, pkg.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, preservationaction.PackageTable, preservationaction.PackageColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTasks chains the current query on the "tasks" edge.
func (paq *PreservationActionQuery) QueryTasks() *PreservationTaskQuery {
	query := (&PreservationTaskClient{config: paq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := paq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(preservationaction.Table, preservationaction.FieldID, selector),
			sqlgraph.To(preservationtask.Table, preservationtask.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, preservationaction.TasksTable, preservationaction.TasksColumn),
		)
		fromU = sqlgraph.SetNeighbors(paq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PreservationAction entity from the query.
// Returns a *NotFoundError when no PreservationAction was found.
func (paq *PreservationActionQuery) First(ctx context.Context) (*PreservationAction, error) {
	nodes, err := paq.Limit(1).All(setContextOp(ctx, paq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{preservationaction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (paq *PreservationActionQuery) FirstX(ctx context.Context) *PreservationAction {
	node, err := paq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PreservationAction ID from the query.
// Returns a *NotFoundError when no PreservationAction ID was found.
func (paq *PreservationActionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = paq.Limit(1).IDs(setContextOp(ctx, paq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{preservationaction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (paq *PreservationActionQuery) FirstIDX(ctx context.Context) int {
	id, err := paq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PreservationAction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PreservationAction entity is found.
// Returns a *NotFoundError when no PreservationAction entities are found.
func (paq *PreservationActionQuery) Only(ctx context.Context) (*PreservationAction, error) {
	nodes, err := paq.Limit(2).All(setContextOp(ctx, paq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{preservationaction.Label}
	default:
		return nil, &NotSingularError{preservationaction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (paq *PreservationActionQuery) OnlyX(ctx context.Context) *PreservationAction {
	node, err := paq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PreservationAction ID in the query.
// Returns a *NotSingularError when more than one PreservationAction ID is found.
// Returns a *NotFoundError when no entities are found.
func (paq *PreservationActionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = paq.Limit(2).IDs(setContextOp(ctx, paq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{preservationaction.Label}
	default:
		err = &NotSingularError{preservationaction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (paq *PreservationActionQuery) OnlyIDX(ctx context.Context) int {
	id, err := paq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PreservationActions.
func (paq *PreservationActionQuery) All(ctx context.Context) ([]*PreservationAction, error) {
	ctx = setContextOp(ctx, paq.ctx, ent.OpQueryAll)
	if err := paq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PreservationAction, *PreservationActionQuery]()
	return withInterceptors[[]*PreservationAction](ctx, paq, qr, paq.inters)
}

// AllX is like All, but panics if an error occurs.
func (paq *PreservationActionQuery) AllX(ctx context.Context) []*PreservationAction {
	nodes, err := paq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PreservationAction IDs.
func (paq *PreservationActionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if paq.ctx.Unique == nil && paq.path != nil {
		paq.Unique(true)
	}
	ctx = setContextOp(ctx, paq.ctx, ent.OpQueryIDs)
	if err = paq.Select(preservationaction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (paq *PreservationActionQuery) IDsX(ctx context.Context) []int {
	ids, err := paq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (paq *PreservationActionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, paq.ctx, ent.OpQueryCount)
	if err := paq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, paq, querierCount[*PreservationActionQuery](), paq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (paq *PreservationActionQuery) CountX(ctx context.Context) int {
	count, err := paq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (paq *PreservationActionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, paq.ctx, ent.OpQueryExist)
	switch _, err := paq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (paq *PreservationActionQuery) ExistX(ctx context.Context) bool {
	exist, err := paq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PreservationActionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (paq *PreservationActionQuery) Clone() *PreservationActionQuery {
	if paq == nil {
		return nil
	}
	return &PreservationActionQuery{
		config:      paq.config,
		ctx:         paq.ctx.Clone(),
		order:       append([]preservationaction.OrderOption{}, paq.order...),
		inters:      append([]Interceptor{}, paq.inters...),
		predicates:  append([]predicate.PreservationAction{}, paq.predicates...),
		withPackage: paq.withPackage.Clone(),
		withTasks:   paq.withTasks.Clone(),
		// clone intermediate query.
		sql:  paq.sql.Clone(),
		path: paq.path,
	}
}

// WithPackage tells the query-builder to eager-load the nodes that are connected to
// the "package" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *PreservationActionQuery) WithPackage(opts ...func(*PkgQuery)) *PreservationActionQuery {
	query := (&PkgClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withPackage = query
	return paq
}

// WithTasks tells the query-builder to eager-load the nodes that are connected to
// the "tasks" edge. The optional arguments are used to configure the query builder of the edge.
func (paq *PreservationActionQuery) WithTasks(opts ...func(*PreservationTaskQuery)) *PreservationActionQuery {
	query := (&PreservationTaskClient{config: paq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	paq.withTasks = query
	return paq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WorkflowID string `json:"workflow_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PreservationAction.Query().
//		GroupBy(preservationaction.FieldWorkflowID).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (paq *PreservationActionQuery) GroupBy(field string, fields ...string) *PreservationActionGroupBy {
	paq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PreservationActionGroupBy{build: paq}
	grbuild.flds = &paq.ctx.Fields
	grbuild.label = preservationaction.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WorkflowID string `json:"workflow_id,omitempty"`
//	}
//
//	client.PreservationAction.Query().
//		Select(preservationaction.FieldWorkflowID).
//		Scan(ctx, &v)
func (paq *PreservationActionQuery) Select(fields ...string) *PreservationActionSelect {
	paq.ctx.Fields = append(paq.ctx.Fields, fields...)
	sbuild := &PreservationActionSelect{PreservationActionQuery: paq}
	sbuild.label = preservationaction.Label
	sbuild.flds, sbuild.scan = &paq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PreservationActionSelect configured with the given aggregations.
func (paq *PreservationActionQuery) Aggregate(fns ...AggregateFunc) *PreservationActionSelect {
	return paq.Select().Aggregate(fns...)
}

func (paq *PreservationActionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range paq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, paq); err != nil {
				return err
			}
		}
	}
	for _, f := range paq.ctx.Fields {
		if !preservationaction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if paq.path != nil {
		prev, err := paq.path(ctx)
		if err != nil {
			return err
		}
		paq.sql = prev
	}
	return nil
}

func (paq *PreservationActionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PreservationAction, error) {
	var (
		nodes       = []*PreservationAction{}
		_spec       = paq.querySpec()
		loadedTypes = [2]bool{
			paq.withPackage != nil,
			paq.withTasks != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PreservationAction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PreservationAction{config: paq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, paq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := paq.withPackage; query != nil {
		if err := paq.loadPackage(ctx, query, nodes, nil,
			func(n *PreservationAction, e *Pkg) { n.Edges.Package = e }); err != nil {
			return nil, err
		}
	}
	if query := paq.withTasks; query != nil {
		if err := paq.loadTasks(ctx, query, nodes,
			func(n *PreservationAction) { n.Edges.Tasks = []*PreservationTask{} },
			func(n *PreservationAction, e *PreservationTask) { n.Edges.Tasks = append(n.Edges.Tasks, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (paq *PreservationActionQuery) loadPackage(ctx context.Context, query *PkgQuery, nodes []*PreservationAction, init func(*PreservationAction), assign func(*PreservationAction, *Pkg)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PreservationAction)
	for i := range nodes {
		fk := nodes[i].PackageID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(pkg.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "package_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (paq *PreservationActionQuery) loadTasks(ctx context.Context, query *PreservationTaskQuery, nodes []*PreservationAction, init func(*PreservationAction), assign func(*PreservationAction, *PreservationTask)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*PreservationAction)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(preservationtask.FieldPreservationActionID)
	}
	query.Where(predicate.PreservationTask(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(preservationaction.TasksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PreservationActionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "preservation_action_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (paq *PreservationActionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := paq.querySpec()
	_spec.Node.Columns = paq.ctx.Fields
	if len(paq.ctx.Fields) > 0 {
		_spec.Unique = paq.ctx.Unique != nil && *paq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, paq.driver, _spec)
}

func (paq *PreservationActionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(preservationaction.Table, preservationaction.Columns, sqlgraph.NewFieldSpec(preservationaction.FieldID, field.TypeInt))
	_spec.From = paq.sql
	if unique := paq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if paq.path != nil {
		_spec.Unique = true
	}
	if fields := paq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, preservationaction.FieldID)
		for i := range fields {
			if fields[i] != preservationaction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if paq.withPackage != nil {
			_spec.Node.AddColumnOnce(preservationaction.FieldPackageID)
		}
	}
	if ps := paq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := paq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := paq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := paq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (paq *PreservationActionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(paq.driver.Dialect())
	t1 := builder.Table(preservationaction.Table)
	columns := paq.ctx.Fields
	if len(columns) == 0 {
		columns = preservationaction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if paq.sql != nil {
		selector = paq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if paq.ctx.Unique != nil && *paq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range paq.predicates {
		p(selector)
	}
	for _, p := range paq.order {
		p(selector)
	}
	if offset := paq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := paq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PreservationActionGroupBy is the group-by builder for PreservationAction entities.
type PreservationActionGroupBy struct {
	selector
	build *PreservationActionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pagb *PreservationActionGroupBy) Aggregate(fns ...AggregateFunc) *PreservationActionGroupBy {
	pagb.fns = append(pagb.fns, fns...)
	return pagb
}

// Scan applies the selector query and scans the result into the given value.
func (pagb *PreservationActionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pagb.build.ctx, ent.OpQueryGroupBy)
	if err := pagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PreservationActionQuery, *PreservationActionGroupBy](ctx, pagb.build, pagb, pagb.build.inters, v)
}

func (pagb *PreservationActionGroupBy) sqlScan(ctx context.Context, root *PreservationActionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pagb.fns))
	for _, fn := range pagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pagb.flds)+len(pagb.fns))
		for _, f := range *pagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PreservationActionSelect is the builder for selecting fields of PreservationAction entities.
type PreservationActionSelect struct {
	*PreservationActionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pas *PreservationActionSelect) Aggregate(fns ...AggregateFunc) *PreservationActionSelect {
	pas.fns = append(pas.fns, fns...)
	return pas
}

// Scan applies the selector query and scans the result into the given value.
func (pas *PreservationActionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pas.ctx, ent.OpQuerySelect)
	if err := pas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PreservationActionQuery, *PreservationActionSelect](ctx, pas.PreservationActionQuery, pas, pas.inters, v)
}

func (pas *PreservationActionSelect) sqlScan(ctx context.Context, root *PreservationActionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pas.fns))
	for _, fn := range pas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
