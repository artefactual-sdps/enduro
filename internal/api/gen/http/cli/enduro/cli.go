// Code generated by goa v3.22.2, DO NOT EDIT.
//
// enduro HTTP client CLI support package
//
// Command:
// $ goa gen github.com/artefactual-sdps/enduro/internal/api/design -o
// internal/api

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	aboutc "github.com/artefactual-sdps/enduro/internal/api/gen/http/about/client"
	ingestc "github.com/artefactual-sdps/enduro/internal/api/gen/http/ingest/client"
	storagec "github.com/artefactual-sdps/enduro/internal/api/gen/http/storage/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"about about",
		"ingest (monitor-request|monitor|list-sips|show-sip|list-sip-workflows|confirm-sip|reject-sip|add-sip|upload-sip|download-sip-request|download-sip|list-users|list-sip-source-objects|add-batch|list-batches|show-batch)",
		"storage (monitor-request|monitor|list-aips|create-aip|submit-aip|update-aip|download-aip-request|download-aip|move-aip|move-aip-status|reject-aip|show-aip|list-aip-workflows|list-deletion-requests|request-aip-deletion|review-aip-deletion|cancel-aip-deletion|download-deletion-report-request|download-deletion-report|list-locations|create-location|show-location|list-location-aips)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` about about --token "abc123"` + "\n" +
		os.Args[0] + ` ingest monitor-request --token "abc123"` + "\n" +
		os.Args[0] + ` storage monitor-request --token "abc123"` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	dialer goahttp.Dialer,
	ingestConfigurer *ingestc.ConnConfigurer,
	storageConfigurer *storagec.ConnConfigurer,
) (goa.Endpoint, any, error) {
	var (
		aboutFlags = flag.NewFlagSet("about", flag.ContinueOnError)

		aboutAboutFlags     = flag.NewFlagSet("about", flag.ExitOnError)
		aboutAboutTokenFlag = aboutAboutFlags.String("token", "", "")

		ingestFlags = flag.NewFlagSet("ingest", flag.ContinueOnError)

		ingestMonitorRequestFlags     = flag.NewFlagSet("monitor-request", flag.ExitOnError)
		ingestMonitorRequestTokenFlag = ingestMonitorRequestFlags.String("token", "", "")

		ingestMonitorFlags      = flag.NewFlagSet("monitor", flag.ExitOnError)
		ingestMonitorTicketFlag = ingestMonitorFlags.String("ticket", "", "")

		ingestListSipsFlags                   = flag.NewFlagSet("list-sips", flag.ExitOnError)
		ingestListSipsNameFlag                = ingestListSipsFlags.String("name", "", "")
		ingestListSipsAipUUIDFlag             = ingestListSipsFlags.String("aip-uuid", "", "")
		ingestListSipsEarliestCreatedTimeFlag = ingestListSipsFlags.String("earliest-created-time", "", "")
		ingestListSipsLatestCreatedTimeFlag   = ingestListSipsFlags.String("latest-created-time", "", "")
		ingestListSipsStatusFlag              = ingestListSipsFlags.String("status", "", "")
		ingestListSipsUploaderUUIDFlag        = ingestListSipsFlags.String("uploader-uuid", "", "")
		ingestListSipsBatchUUIDFlag           = ingestListSipsFlags.String("batch-uuid", "", "")
		ingestListSipsLimitFlag               = ingestListSipsFlags.String("limit", "", "")
		ingestListSipsOffsetFlag              = ingestListSipsFlags.String("offset", "", "")
		ingestListSipsTokenFlag               = ingestListSipsFlags.String("token", "", "")

		ingestShowSipFlags     = flag.NewFlagSet("show-sip", flag.ExitOnError)
		ingestShowSipUUIDFlag  = ingestShowSipFlags.String("uuid", "REQUIRED", "Identifier of SIP to show")
		ingestShowSipTokenFlag = ingestShowSipFlags.String("token", "", "")

		ingestListSipWorkflowsFlags     = flag.NewFlagSet("list-sip-workflows", flag.ExitOnError)
		ingestListSipWorkflowsUUIDFlag  = ingestListSipWorkflowsFlags.String("uuid", "REQUIRED", "Identifier of SIP to look up")
		ingestListSipWorkflowsTokenFlag = ingestListSipWorkflowsFlags.String("token", "", "")

		ingestConfirmSipFlags     = flag.NewFlagSet("confirm-sip", flag.ExitOnError)
		ingestConfirmSipBodyFlag  = ingestConfirmSipFlags.String("body", "REQUIRED", "")
		ingestConfirmSipUUIDFlag  = ingestConfirmSipFlags.String("uuid", "REQUIRED", "Identifier of SIP to look up")
		ingestConfirmSipTokenFlag = ingestConfirmSipFlags.String("token", "", "")

		ingestRejectSipFlags     = flag.NewFlagSet("reject-sip", flag.ExitOnError)
		ingestRejectSipUUIDFlag  = ingestRejectSipFlags.String("uuid", "REQUIRED", "Identifier of SIP to look up")
		ingestRejectSipTokenFlag = ingestRejectSipFlags.String("token", "", "")

		ingestAddSipFlags        = flag.NewFlagSet("add-sip", flag.ExitOnError)
		ingestAddSipSourceIDFlag = ingestAddSipFlags.String("source-id", "REQUIRED", "")
		ingestAddSipKeyFlag      = ingestAddSipFlags.String("key", "REQUIRED", "")
		ingestAddSipTokenFlag    = ingestAddSipFlags.String("token", "", "")

		ingestUploadSipFlags           = flag.NewFlagSet("upload-sip", flag.ExitOnError)
		ingestUploadSipContentTypeFlag = ingestUploadSipFlags.String("content-type", "multipart/form-data; boundary=goa", "")
		ingestUploadSipTokenFlag       = ingestUploadSipFlags.String("token", "", "")
		ingestUploadSipStreamFlag      = ingestUploadSipFlags.String("stream", "REQUIRED", "path to file containing the streamed request body")

		ingestDownloadSipRequestFlags     = flag.NewFlagSet("download-sip-request", flag.ExitOnError)
		ingestDownloadSipRequestUUIDFlag  = ingestDownloadSipRequestFlags.String("uuid", "REQUIRED", "Identifier of the SIP to download")
		ingestDownloadSipRequestTokenFlag = ingestDownloadSipRequestFlags.String("token", "", "")

		ingestDownloadSipFlags      = flag.NewFlagSet("download-sip", flag.ExitOnError)
		ingestDownloadSipUUIDFlag   = ingestDownloadSipFlags.String("uuid", "REQUIRED", "Identifier of the SIP to download")
		ingestDownloadSipTicketFlag = ingestDownloadSipFlags.String("ticket", "", "")

		ingestListUsersFlags      = flag.NewFlagSet("list-users", flag.ExitOnError)
		ingestListUsersEmailFlag  = ingestListUsersFlags.String("email", "", "")
		ingestListUsersNameFlag   = ingestListUsersFlags.String("name", "", "")
		ingestListUsersLimitFlag  = ingestListUsersFlags.String("limit", "", "")
		ingestListUsersOffsetFlag = ingestListUsersFlags.String("offset", "", "")
		ingestListUsersTokenFlag  = ingestListUsersFlags.String("token", "", "")

		ingestListSipSourceObjectsFlags      = flag.NewFlagSet("list-sip-source-objects", flag.ExitOnError)
		ingestListSipSourceObjectsUUIDFlag   = ingestListSipSourceObjectsFlags.String("uuid", "REQUIRED", "SIP source identifier -- CURRENTLY NOT USED")
		ingestListSipSourceObjectsLimitFlag  = ingestListSipSourceObjectsFlags.String("limit", "", "")
		ingestListSipSourceObjectsCursorFlag = ingestListSipSourceObjectsFlags.String("cursor", "", "")
		ingestListSipSourceObjectsTokenFlag  = ingestListSipSourceObjectsFlags.String("token", "", "")

		ingestAddBatchFlags     = flag.NewFlagSet("add-batch", flag.ExitOnError)
		ingestAddBatchBodyFlag  = ingestAddBatchFlags.String("body", "REQUIRED", "")
		ingestAddBatchTokenFlag = ingestAddBatchFlags.String("token", "", "")

		ingestListBatchesFlags                   = flag.NewFlagSet("list-batches", flag.ExitOnError)
		ingestListBatchesIdentifierFlag          = ingestListBatchesFlags.String("identifier", "", "")
		ingestListBatchesEarliestCreatedTimeFlag = ingestListBatchesFlags.String("earliest-created-time", "", "")
		ingestListBatchesLatestCreatedTimeFlag   = ingestListBatchesFlags.String("latest-created-time", "", "")
		ingestListBatchesStatusFlag              = ingestListBatchesFlags.String("status", "", "")
		ingestListBatchesUploaderUUIDFlag        = ingestListBatchesFlags.String("uploader-uuid", "", "")
		ingestListBatchesLimitFlag               = ingestListBatchesFlags.String("limit", "", "")
		ingestListBatchesOffsetFlag              = ingestListBatchesFlags.String("offset", "", "")
		ingestListBatchesTokenFlag               = ingestListBatchesFlags.String("token", "", "")

		ingestShowBatchFlags     = flag.NewFlagSet("show-batch", flag.ExitOnError)
		ingestShowBatchUUIDFlag  = ingestShowBatchFlags.String("uuid", "REQUIRED", "Identifier of Batch to show")
		ingestShowBatchTokenFlag = ingestShowBatchFlags.String("token", "", "")

		storageFlags = flag.NewFlagSet("storage", flag.ContinueOnError)

		storageMonitorRequestFlags     = flag.NewFlagSet("monitor-request", flag.ExitOnError)
		storageMonitorRequestTokenFlag = storageMonitorRequestFlags.String("token", "", "")

		storageMonitorFlags      = flag.NewFlagSet("monitor", flag.ExitOnError)
		storageMonitorTicketFlag = storageMonitorFlags.String("ticket", "", "")

		storageListAipsFlags                   = flag.NewFlagSet("list-aips", flag.ExitOnError)
		storageListAipsNameFlag                = storageListAipsFlags.String("name", "", "")
		storageListAipsEarliestCreatedTimeFlag = storageListAipsFlags.String("earliest-created-time", "", "")
		storageListAipsLatestCreatedTimeFlag   = storageListAipsFlags.String("latest-created-time", "", "")
		storageListAipsStatusFlag              = storageListAipsFlags.String("status", "", "")
		storageListAipsLimitFlag               = storageListAipsFlags.String("limit", "", "")
		storageListAipsOffsetFlag              = storageListAipsFlags.String("offset", "", "")
		storageListAipsTokenFlag               = storageListAipsFlags.String("token", "", "")

		storageCreateAipFlags     = flag.NewFlagSet("create-aip", flag.ExitOnError)
		storageCreateAipBodyFlag  = storageCreateAipFlags.String("body", "REQUIRED", "")
		storageCreateAipTokenFlag = storageCreateAipFlags.String("token", "", "")

		storageSubmitAipFlags     = flag.NewFlagSet("submit-aip", flag.ExitOnError)
		storageSubmitAipBodyFlag  = storageSubmitAipFlags.String("body", "REQUIRED", "")
		storageSubmitAipUUIDFlag  = storageSubmitAipFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageSubmitAipTokenFlag = storageSubmitAipFlags.String("token", "", "")

		storageUpdateAipFlags     = flag.NewFlagSet("update-aip", flag.ExitOnError)
		storageUpdateAipUUIDFlag  = storageUpdateAipFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageUpdateAipTokenFlag = storageUpdateAipFlags.String("token", "", "")

		storageDownloadAipRequestFlags     = flag.NewFlagSet("download-aip-request", flag.ExitOnError)
		storageDownloadAipRequestUUIDFlag  = storageDownloadAipRequestFlags.String("uuid", "REQUIRED", "Identifier of the AIP to download")
		storageDownloadAipRequestTokenFlag = storageDownloadAipRequestFlags.String("token", "", "")

		storageDownloadAipFlags      = flag.NewFlagSet("download-aip", flag.ExitOnError)
		storageDownloadAipUUIDFlag   = storageDownloadAipFlags.String("uuid", "REQUIRED", "Identifier of the AIP to download")
		storageDownloadAipTicketFlag = storageDownloadAipFlags.String("ticket", "", "")

		storageMoveAipFlags     = flag.NewFlagSet("move-aip", flag.ExitOnError)
		storageMoveAipBodyFlag  = storageMoveAipFlags.String("body", "REQUIRED", "")
		storageMoveAipUUIDFlag  = storageMoveAipFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageMoveAipTokenFlag = storageMoveAipFlags.String("token", "", "")

		storageMoveAipStatusFlags     = flag.NewFlagSet("move-aip-status", flag.ExitOnError)
		storageMoveAipStatusUUIDFlag  = storageMoveAipStatusFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageMoveAipStatusTokenFlag = storageMoveAipStatusFlags.String("token", "", "")

		storageRejectAipFlags     = flag.NewFlagSet("reject-aip", flag.ExitOnError)
		storageRejectAipUUIDFlag  = storageRejectAipFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageRejectAipTokenFlag = storageRejectAipFlags.String("token", "", "")

		storageShowAipFlags     = flag.NewFlagSet("show-aip", flag.ExitOnError)
		storageShowAipUUIDFlag  = storageShowAipFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageShowAipTokenFlag = storageShowAipFlags.String("token", "", "")

		storageListAipWorkflowsFlags      = flag.NewFlagSet("list-aip-workflows", flag.ExitOnError)
		storageListAipWorkflowsUUIDFlag   = storageListAipWorkflowsFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageListAipWorkflowsStatusFlag = storageListAipWorkflowsFlags.String("status", "", "")
		storageListAipWorkflowsTypeFlag   = storageListAipWorkflowsFlags.String("type", "", "")
		storageListAipWorkflowsTokenFlag  = storageListAipWorkflowsFlags.String("token", "", "")

		storageListDeletionRequestsFlags      = flag.NewFlagSet("list-deletion-requests", flag.ExitOnError)
		storageListDeletionRequestsUUIDFlag   = storageListDeletionRequestsFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageListDeletionRequestsStatusFlag = storageListDeletionRequestsFlags.String("status", "", "")
		storageListDeletionRequestsTokenFlag  = storageListDeletionRequestsFlags.String("token", "", "")

		storageRequestAipDeletionFlags     = flag.NewFlagSet("request-aip-deletion", flag.ExitOnError)
		storageRequestAipDeletionBodyFlag  = storageRequestAipDeletionFlags.String("body", "REQUIRED", "")
		storageRequestAipDeletionUUIDFlag  = storageRequestAipDeletionFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageRequestAipDeletionTokenFlag = storageRequestAipDeletionFlags.String("token", "", "")

		storageReviewAipDeletionFlags     = flag.NewFlagSet("review-aip-deletion", flag.ExitOnError)
		storageReviewAipDeletionBodyFlag  = storageReviewAipDeletionFlags.String("body", "REQUIRED", "")
		storageReviewAipDeletionUUIDFlag  = storageReviewAipDeletionFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageReviewAipDeletionTokenFlag = storageReviewAipDeletionFlags.String("token", "", "")

		storageCancelAipDeletionFlags     = flag.NewFlagSet("cancel-aip-deletion", flag.ExitOnError)
		storageCancelAipDeletionBodyFlag  = storageCancelAipDeletionFlags.String("body", "REQUIRED", "")
		storageCancelAipDeletionUUIDFlag  = storageCancelAipDeletionFlags.String("uuid", "REQUIRED", "Identifier of AIP")
		storageCancelAipDeletionTokenFlag = storageCancelAipDeletionFlags.String("token", "", "")

		storageDownloadDeletionReportRequestFlags     = flag.NewFlagSet("download-deletion-report-request", flag.ExitOnError)
		storageDownloadDeletionReportRequestUUIDFlag  = storageDownloadDeletionReportRequestFlags.String("uuid", "REQUIRED", "UUID of the deletion report to download")
		storageDownloadDeletionReportRequestTokenFlag = storageDownloadDeletionReportRequestFlags.String("token", "", "")

		storageDownloadDeletionReportFlags      = flag.NewFlagSet("download-deletion-report", flag.ExitOnError)
		storageDownloadDeletionReportUUIDFlag   = storageDownloadDeletionReportFlags.String("uuid", "REQUIRED", "UUID of the deletion report to download")
		storageDownloadDeletionReportTicketFlag = storageDownloadDeletionReportFlags.String("ticket", "", "")

		storageListLocationsFlags     = flag.NewFlagSet("list-locations", flag.ExitOnError)
		storageListLocationsTokenFlag = storageListLocationsFlags.String("token", "", "")

		storageCreateLocationFlags     = flag.NewFlagSet("create-location", flag.ExitOnError)
		storageCreateLocationBodyFlag  = storageCreateLocationFlags.String("body", "REQUIRED", "")
		storageCreateLocationTokenFlag = storageCreateLocationFlags.String("token", "", "")

		storageShowLocationFlags     = flag.NewFlagSet("show-location", flag.ExitOnError)
		storageShowLocationUUIDFlag  = storageShowLocationFlags.String("uuid", "REQUIRED", "Identifier of location")
		storageShowLocationTokenFlag = storageShowLocationFlags.String("token", "", "")

		storageListLocationAipsFlags     = flag.NewFlagSet("list-location-aips", flag.ExitOnError)
		storageListLocationAipsUUIDFlag  = storageListLocationAipsFlags.String("uuid", "REQUIRED", "Identifier of location")
		storageListLocationAipsTokenFlag = storageListLocationAipsFlags.String("token", "", "")
	)
	aboutFlags.Usage = aboutUsage
	aboutAboutFlags.Usage = aboutAboutUsage

	ingestFlags.Usage = ingestUsage
	ingestMonitorRequestFlags.Usage = ingestMonitorRequestUsage
	ingestMonitorFlags.Usage = ingestMonitorUsage
	ingestListSipsFlags.Usage = ingestListSipsUsage
	ingestShowSipFlags.Usage = ingestShowSipUsage
	ingestListSipWorkflowsFlags.Usage = ingestListSipWorkflowsUsage
	ingestConfirmSipFlags.Usage = ingestConfirmSipUsage
	ingestRejectSipFlags.Usage = ingestRejectSipUsage
	ingestAddSipFlags.Usage = ingestAddSipUsage
	ingestUploadSipFlags.Usage = ingestUploadSipUsage
	ingestDownloadSipRequestFlags.Usage = ingestDownloadSipRequestUsage
	ingestDownloadSipFlags.Usage = ingestDownloadSipUsage
	ingestListUsersFlags.Usage = ingestListUsersUsage
	ingestListSipSourceObjectsFlags.Usage = ingestListSipSourceObjectsUsage
	ingestAddBatchFlags.Usage = ingestAddBatchUsage
	ingestListBatchesFlags.Usage = ingestListBatchesUsage
	ingestShowBatchFlags.Usage = ingestShowBatchUsage

	storageFlags.Usage = storageUsage
	storageMonitorRequestFlags.Usage = storageMonitorRequestUsage
	storageMonitorFlags.Usage = storageMonitorUsage
	storageListAipsFlags.Usage = storageListAipsUsage
	storageCreateAipFlags.Usage = storageCreateAipUsage
	storageSubmitAipFlags.Usage = storageSubmitAipUsage
	storageUpdateAipFlags.Usage = storageUpdateAipUsage
	storageDownloadAipRequestFlags.Usage = storageDownloadAipRequestUsage
	storageDownloadAipFlags.Usage = storageDownloadAipUsage
	storageMoveAipFlags.Usage = storageMoveAipUsage
	storageMoveAipStatusFlags.Usage = storageMoveAipStatusUsage
	storageRejectAipFlags.Usage = storageRejectAipUsage
	storageShowAipFlags.Usage = storageShowAipUsage
	storageListAipWorkflowsFlags.Usage = storageListAipWorkflowsUsage
	storageListDeletionRequestsFlags.Usage = storageListDeletionRequestsUsage
	storageRequestAipDeletionFlags.Usage = storageRequestAipDeletionUsage
	storageReviewAipDeletionFlags.Usage = storageReviewAipDeletionUsage
	storageCancelAipDeletionFlags.Usage = storageCancelAipDeletionUsage
	storageDownloadDeletionReportRequestFlags.Usage = storageDownloadDeletionReportRequestUsage
	storageDownloadDeletionReportFlags.Usage = storageDownloadDeletionReportUsage
	storageListLocationsFlags.Usage = storageListLocationsUsage
	storageCreateLocationFlags.Usage = storageCreateLocationUsage
	storageShowLocationFlags.Usage = storageShowLocationUsage
	storageListLocationAipsFlags.Usage = storageListLocationAipsUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "about":
			svcf = aboutFlags
		case "ingest":
			svcf = ingestFlags
		case "storage":
			svcf = storageFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "about":
			switch epn {
			case "about":
				epf = aboutAboutFlags

			}

		case "ingest":
			switch epn {
			case "monitor-request":
				epf = ingestMonitorRequestFlags

			case "monitor":
				epf = ingestMonitorFlags

			case "list-sips":
				epf = ingestListSipsFlags

			case "show-sip":
				epf = ingestShowSipFlags

			case "list-sip-workflows":
				epf = ingestListSipWorkflowsFlags

			case "confirm-sip":
				epf = ingestConfirmSipFlags

			case "reject-sip":
				epf = ingestRejectSipFlags

			case "add-sip":
				epf = ingestAddSipFlags

			case "upload-sip":
				epf = ingestUploadSipFlags

			case "download-sip-request":
				epf = ingestDownloadSipRequestFlags

			case "download-sip":
				epf = ingestDownloadSipFlags

			case "list-users":
				epf = ingestListUsersFlags

			case "list-sip-source-objects":
				epf = ingestListSipSourceObjectsFlags

			case "add-batch":
				epf = ingestAddBatchFlags

			case "list-batches":
				epf = ingestListBatchesFlags

			case "show-batch":
				epf = ingestShowBatchFlags

			}

		case "storage":
			switch epn {
			case "monitor-request":
				epf = storageMonitorRequestFlags

			case "monitor":
				epf = storageMonitorFlags

			case "list-aips":
				epf = storageListAipsFlags

			case "create-aip":
				epf = storageCreateAipFlags

			case "submit-aip":
				epf = storageSubmitAipFlags

			case "update-aip":
				epf = storageUpdateAipFlags

			case "download-aip-request":
				epf = storageDownloadAipRequestFlags

			case "download-aip":
				epf = storageDownloadAipFlags

			case "move-aip":
				epf = storageMoveAipFlags

			case "move-aip-status":
				epf = storageMoveAipStatusFlags

			case "reject-aip":
				epf = storageRejectAipFlags

			case "show-aip":
				epf = storageShowAipFlags

			case "list-aip-workflows":
				epf = storageListAipWorkflowsFlags

			case "list-deletion-requests":
				epf = storageListDeletionRequestsFlags

			case "request-aip-deletion":
				epf = storageRequestAipDeletionFlags

			case "review-aip-deletion":
				epf = storageReviewAipDeletionFlags

			case "cancel-aip-deletion":
				epf = storageCancelAipDeletionFlags

			case "download-deletion-report-request":
				epf = storageDownloadDeletionReportRequestFlags

			case "download-deletion-report":
				epf = storageDownloadDeletionReportFlags

			case "list-locations":
				epf = storageListLocationsFlags

			case "create-location":
				epf = storageCreateLocationFlags

			case "show-location":
				epf = storageShowLocationFlags

			case "list-location-aips":
				epf = storageListLocationAipsFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "about":
			c := aboutc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "about":
				endpoint = c.About()
				data, err = aboutc.BuildAboutPayload(*aboutAboutTokenFlag)
			}
		case "ingest":
			c := ingestc.NewClient(scheme, host, doer, enc, dec, restore, dialer, ingestConfigurer)
			switch epn {
			case "monitor-request":
				endpoint = c.MonitorRequest()
				data, err = ingestc.BuildMonitorRequestPayload(*ingestMonitorRequestTokenFlag)
			case "monitor":
				endpoint = c.Monitor()
				data, err = ingestc.BuildMonitorPayload(*ingestMonitorTicketFlag)
			case "list-sips":
				endpoint = c.ListSips()
				data, err = ingestc.BuildListSipsPayload(*ingestListSipsNameFlag, *ingestListSipsAipUUIDFlag, *ingestListSipsEarliestCreatedTimeFlag, *ingestListSipsLatestCreatedTimeFlag, *ingestListSipsStatusFlag, *ingestListSipsUploaderUUIDFlag, *ingestListSipsBatchUUIDFlag, *ingestListSipsLimitFlag, *ingestListSipsOffsetFlag, *ingestListSipsTokenFlag)
			case "show-sip":
				endpoint = c.ShowSip()
				data, err = ingestc.BuildShowSipPayload(*ingestShowSipUUIDFlag, *ingestShowSipTokenFlag)
			case "list-sip-workflows":
				endpoint = c.ListSipWorkflows()
				data, err = ingestc.BuildListSipWorkflowsPayload(*ingestListSipWorkflowsUUIDFlag, *ingestListSipWorkflowsTokenFlag)
			case "confirm-sip":
				endpoint = c.ConfirmSip()
				data, err = ingestc.BuildConfirmSipPayload(*ingestConfirmSipBodyFlag, *ingestConfirmSipUUIDFlag, *ingestConfirmSipTokenFlag)
			case "reject-sip":
				endpoint = c.RejectSip()
				data, err = ingestc.BuildRejectSipPayload(*ingestRejectSipUUIDFlag, *ingestRejectSipTokenFlag)
			case "add-sip":
				endpoint = c.AddSip()
				data, err = ingestc.BuildAddSipPayload(*ingestAddSipSourceIDFlag, *ingestAddSipKeyFlag, *ingestAddSipTokenFlag)
			case "upload-sip":
				endpoint = c.UploadSip()
				data, err = ingestc.BuildUploadSipPayload(*ingestUploadSipContentTypeFlag, *ingestUploadSipTokenFlag)
				if err == nil {
					data, err = ingestc.BuildUploadSipStreamPayload(data, *ingestUploadSipStreamFlag)
				}
			case "download-sip-request":
				endpoint = c.DownloadSipRequest()
				data, err = ingestc.BuildDownloadSipRequestPayload(*ingestDownloadSipRequestUUIDFlag, *ingestDownloadSipRequestTokenFlag)
			case "download-sip":
				endpoint = c.DownloadSip()
				data, err = ingestc.BuildDownloadSipPayload(*ingestDownloadSipUUIDFlag, *ingestDownloadSipTicketFlag)
			case "list-users":
				endpoint = c.ListUsers()
				data, err = ingestc.BuildListUsersPayload(*ingestListUsersEmailFlag, *ingestListUsersNameFlag, *ingestListUsersLimitFlag, *ingestListUsersOffsetFlag, *ingestListUsersTokenFlag)
			case "list-sip-source-objects":
				endpoint = c.ListSipSourceObjects()
				data, err = ingestc.BuildListSipSourceObjectsPayload(*ingestListSipSourceObjectsUUIDFlag, *ingestListSipSourceObjectsLimitFlag, *ingestListSipSourceObjectsCursorFlag, *ingestListSipSourceObjectsTokenFlag)
			case "add-batch":
				endpoint = c.AddBatch()
				data, err = ingestc.BuildAddBatchPayload(*ingestAddBatchBodyFlag, *ingestAddBatchTokenFlag)
			case "list-batches":
				endpoint = c.ListBatches()
				data, err = ingestc.BuildListBatchesPayload(*ingestListBatchesIdentifierFlag, *ingestListBatchesEarliestCreatedTimeFlag, *ingestListBatchesLatestCreatedTimeFlag, *ingestListBatchesStatusFlag, *ingestListBatchesUploaderUUIDFlag, *ingestListBatchesLimitFlag, *ingestListBatchesOffsetFlag, *ingestListBatchesTokenFlag)
			case "show-batch":
				endpoint = c.ShowBatch()
				data, err = ingestc.BuildShowBatchPayload(*ingestShowBatchUUIDFlag, *ingestShowBatchTokenFlag)
			}
		case "storage":
			c := storagec.NewClient(scheme, host, doer, enc, dec, restore, dialer, storageConfigurer)
			switch epn {
			case "monitor-request":
				endpoint = c.MonitorRequest()
				data, err = storagec.BuildMonitorRequestPayload(*storageMonitorRequestTokenFlag)
			case "monitor":
				endpoint = c.Monitor()
				data, err = storagec.BuildMonitorPayload(*storageMonitorTicketFlag)
			case "list-aips":
				endpoint = c.ListAips()
				data, err = storagec.BuildListAipsPayload(*storageListAipsNameFlag, *storageListAipsEarliestCreatedTimeFlag, *storageListAipsLatestCreatedTimeFlag, *storageListAipsStatusFlag, *storageListAipsLimitFlag, *storageListAipsOffsetFlag, *storageListAipsTokenFlag)
			case "create-aip":
				endpoint = c.CreateAip()
				data, err = storagec.BuildCreateAipPayload(*storageCreateAipBodyFlag, *storageCreateAipTokenFlag)
			case "submit-aip":
				endpoint = c.SubmitAip()
				data, err = storagec.BuildSubmitAipPayload(*storageSubmitAipBodyFlag, *storageSubmitAipUUIDFlag, *storageSubmitAipTokenFlag)
			case "update-aip":
				endpoint = c.UpdateAip()
				data, err = storagec.BuildUpdateAipPayload(*storageUpdateAipUUIDFlag, *storageUpdateAipTokenFlag)
			case "download-aip-request":
				endpoint = c.DownloadAipRequest()
				data, err = storagec.BuildDownloadAipRequestPayload(*storageDownloadAipRequestUUIDFlag, *storageDownloadAipRequestTokenFlag)
			case "download-aip":
				endpoint = c.DownloadAip()
				data, err = storagec.BuildDownloadAipPayload(*storageDownloadAipUUIDFlag, *storageDownloadAipTicketFlag)
			case "move-aip":
				endpoint = c.MoveAip()
				data, err = storagec.BuildMoveAipPayload(*storageMoveAipBodyFlag, *storageMoveAipUUIDFlag, *storageMoveAipTokenFlag)
			case "move-aip-status":
				endpoint = c.MoveAipStatus()
				data, err = storagec.BuildMoveAipStatusPayload(*storageMoveAipStatusUUIDFlag, *storageMoveAipStatusTokenFlag)
			case "reject-aip":
				endpoint = c.RejectAip()
				data, err = storagec.BuildRejectAipPayload(*storageRejectAipUUIDFlag, *storageRejectAipTokenFlag)
			case "show-aip":
				endpoint = c.ShowAip()
				data, err = storagec.BuildShowAipPayload(*storageShowAipUUIDFlag, *storageShowAipTokenFlag)
			case "list-aip-workflows":
				endpoint = c.ListAipWorkflows()
				data, err = storagec.BuildListAipWorkflowsPayload(*storageListAipWorkflowsUUIDFlag, *storageListAipWorkflowsStatusFlag, *storageListAipWorkflowsTypeFlag, *storageListAipWorkflowsTokenFlag)
			case "list-deletion-requests":
				endpoint = c.ListDeletionRequests()
				data, err = storagec.BuildListDeletionRequestsPayload(*storageListDeletionRequestsUUIDFlag, *storageListDeletionRequestsStatusFlag, *storageListDeletionRequestsTokenFlag)
			case "request-aip-deletion":
				endpoint = c.RequestAipDeletion()
				data, err = storagec.BuildRequestAipDeletionPayload(*storageRequestAipDeletionBodyFlag, *storageRequestAipDeletionUUIDFlag, *storageRequestAipDeletionTokenFlag)
			case "review-aip-deletion":
				endpoint = c.ReviewAipDeletion()
				data, err = storagec.BuildReviewAipDeletionPayload(*storageReviewAipDeletionBodyFlag, *storageReviewAipDeletionUUIDFlag, *storageReviewAipDeletionTokenFlag)
			case "cancel-aip-deletion":
				endpoint = c.CancelAipDeletion()
				data, err = storagec.BuildCancelAipDeletionPayload(*storageCancelAipDeletionBodyFlag, *storageCancelAipDeletionUUIDFlag, *storageCancelAipDeletionTokenFlag)
			case "download-deletion-report-request":
				endpoint = c.DownloadDeletionReportRequest()
				data, err = storagec.BuildDownloadDeletionReportRequestPayload(*storageDownloadDeletionReportRequestUUIDFlag, *storageDownloadDeletionReportRequestTokenFlag)
			case "download-deletion-report":
				endpoint = c.DownloadDeletionReport()
				data, err = storagec.BuildDownloadDeletionReportPayload(*storageDownloadDeletionReportUUIDFlag, *storageDownloadDeletionReportTicketFlag)
			case "list-locations":
				endpoint = c.ListLocations()
				data, err = storagec.BuildListLocationsPayload(*storageListLocationsTokenFlag)
			case "create-location":
				endpoint = c.CreateLocation()
				data, err = storagec.BuildCreateLocationPayload(*storageCreateLocationBodyFlag, *storageCreateLocationTokenFlag)
			case "show-location":
				endpoint = c.ShowLocation()
				data, err = storagec.BuildShowLocationPayload(*storageShowLocationUUIDFlag, *storageShowLocationTokenFlag)
			case "list-location-aips":
				endpoint = c.ListLocationAips()
				data, err = storagec.BuildListLocationAipsPayload(*storageListLocationAipsUUIDFlag, *storageListLocationAipsTokenFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// aboutUsage displays the usage of the about command and its subcommands.
func aboutUsage() {
	fmt.Fprintln(os.Stderr, `The about service provides information about the system.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] about COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    about: Get information about the system`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s about COMMAND --help\n", os.Args[0])
}
func aboutAboutUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] about about", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get information about the system`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `about about --token "abc123"`)
}

// ingestUsage displays the usage of the ingest command and its subcommands.
func ingestUsage() {
	fmt.Fprintln(os.Stderr, `The ingest service manages ingested SIPs.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] ingest COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    monitor-request: Request access to the /monitor WebSocket`)
	fmt.Fprintln(os.Stderr, `    monitor: Obtain access to the /monitor WebSocket`)
	fmt.Fprintln(os.Stderr, `    list-sips: List all ingested SIPs`)
	fmt.Fprintln(os.Stderr, `    show-sip: Show SIP by ID`)
	fmt.Fprintln(os.Stderr, `    list-sip-workflows: List all workflows for a SIP`)
	fmt.Fprintln(os.Stderr, `    confirm-sip: Signal the SIP has been reviewed and accepted`)
	fmt.Fprintln(os.Stderr, `    reject-sip: Signal the SIP has been reviewed and rejected`)
	fmt.Fprintln(os.Stderr, `    add-sip: Ingest a SIP from a SIP Source`)
	fmt.Fprintln(os.Stderr, `    upload-sip: Upload a SIP to trigger an ingest workflow`)
	fmt.Fprintln(os.Stderr, `    download-sip-request: Request access to SIP download`)
	fmt.Fprintln(os.Stderr, `    download-sip: Download the failed package related to a SIP. It will be the original SIP or the transformed PIP, based on the SIP's `+"`"+`failed_as`+"`"+` value.`)
	fmt.Fprintln(os.Stderr, `    list-users: List all users`)
	fmt.Fprintln(os.Stderr, `    list-sip-source-objects: List the objects in a SIP source`)
	fmt.Fprintln(os.Stderr, `    add-batch: Ingest a Batch from a SIP Source`)
	fmt.Fprintln(os.Stderr, `    list-batches: List all ingested Batches`)
	fmt.Fprintln(os.Stderr, `    show-batch: Show Batch by UUID`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s ingest COMMAND --help\n", os.Args[0])
}
func ingestMonitorRequestUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest monitor-request", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request access to the /monitor WebSocket`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest monitor-request --token "abc123"`)
}

func ingestMonitorUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest monitor", os.Args[0])
	fmt.Fprint(os.Stderr, " -ticket STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Obtain access to the /monitor WebSocket`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -ticket STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest monitor --ticket "abc123"`)
}

func ingestListSipsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest list-sips", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -aip-uuid STRING")
	fmt.Fprint(os.Stderr, " -earliest-created-time STRING")
	fmt.Fprint(os.Stderr, " -latest-created-time STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -uploader-uuid STRING")
	fmt.Fprint(os.Stderr, " -batch-uuid STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprint(os.Stderr, " -offset INT")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all ingested SIPs`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -aip-uuid STRING: `)
	fmt.Fprintln(os.Stderr, `    -earliest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -latest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -uploader-uuid STRING: `)
	fmt.Fprintln(os.Stderr, `    -batch-uuid STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)
	fmt.Fprintln(os.Stderr, `    -offset INT: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest list-sips --name "abc123" --aip-uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --earliest-created-time "1970-01-01T00:00:01Z" --latest-created-time "1970-01-01T00:00:01Z" --status "failed" --uploader-uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --batch-uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --limit 1 --offset 1 --token "abc123"`)
}

func ingestShowSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest show-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show SIP by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of SIP to show`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest show-sip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func ingestListSipWorkflowsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest list-sip-workflows", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all workflows for a SIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of SIP to look up`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest list-sip-workflows --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func ingestConfirmSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest confirm-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Signal the SIP has been reviewed and accepted`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of SIP to look up`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest confirm-sip --body '{
      "location_uuid": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5"
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func ingestRejectSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest reject-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Signal the SIP has been reviewed and rejected`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of SIP to look up`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest reject-sip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func ingestAddSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest add-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -source-id STRING")
	fmt.Fprint(os.Stderr, " -key STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Ingest a SIP from a SIP Source`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -source-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -key STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest add-sip --source-id "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --key "abc123" --token "abc123"`)
}

func ingestUploadSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest upload-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -content-type STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprint(os.Stderr, " -stream STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Upload a SIP to trigger an ingest workflow`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -content-type STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)
	fmt.Fprintln(os.Stderr, `    -stream STRING: path to file containing the streamed request body`)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest upload-sip --content-type "multipart/form-data; boundary=goa" --token "abc123" --stream "goa.png"`)
}

func ingestDownloadSipRequestUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest download-sip-request", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request access to SIP download`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of the SIP to download`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest download-sip-request --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func ingestDownloadSipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest download-sip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -ticket STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Download the failed package related to a SIP. It will be the original SIP or the transformed PIP, based on the SIP's `+"`"+`failed_as`+"`"+` value.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of the SIP to download`)
	fmt.Fprintln(os.Stderr, `    -ticket STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest download-sip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --ticket "abc123"`)
}

func ingestListUsersUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest list-users", os.Args[0])
	fmt.Fprint(os.Stderr, " -email STRING")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprint(os.Stderr, " -offset INT")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all users`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -email STRING: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)
	fmt.Fprintln(os.Stderr, `    -offset INT: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest list-users --email "nobody@example.com" --name "Jane Doe" --limit 1 --offset 1 --token "abc123"`)
}

func ingestListSipSourceObjectsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest list-sip-source-objects", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprint(os.Stderr, " -cursor STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List the objects in a SIP source`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: SIP source identifier -- CURRENTLY NOT USED`)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)
	fmt.Fprintln(os.Stderr, `    -cursor STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest list-sip-source-objects --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --limit 1 --cursor "abc123" --token "abc123"`)
}

func ingestAddBatchUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest add-batch", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Ingest a Batch from a SIP Source`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest add-batch --body '{
      "identifier": "abc123",
      "keys": [
         "abc123"
      ],
      "source_id": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5"
   }' --token "abc123"`)
}

func ingestListBatchesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest list-batches", os.Args[0])
	fmt.Fprint(os.Stderr, " -identifier STRING")
	fmt.Fprint(os.Stderr, " -earliest-created-time STRING")
	fmt.Fprint(os.Stderr, " -latest-created-time STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -uploader-uuid STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprint(os.Stderr, " -offset INT")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all ingested Batches`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -identifier STRING: `)
	fmt.Fprintln(os.Stderr, `    -earliest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -latest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -uploader-uuid STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)
	fmt.Fprintln(os.Stderr, `    -offset INT: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest list-batches --identifier "abc123" --earliest-created-time "1970-01-01T00:00:01Z" --latest-created-time "1970-01-01T00:00:01Z" --status "processing" --uploader-uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --limit 1 --offset 1 --token "abc123"`)
}

func ingestShowBatchUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] ingest show-batch", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show Batch by UUID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of Batch to show`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `ingest show-batch --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

// storageUsage displays the usage of the storage command and its subcommands.
func storageUsage() {
	fmt.Fprintln(os.Stderr, `The storage service manages locations and AIPs.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] storage COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    monitor-request: Request access to the /monitor WebSocket`)
	fmt.Fprintln(os.Stderr, `    monitor: Obtain access to the /monitor WebSocket`)
	fmt.Fprintln(os.Stderr, `    list-aips: List all AIPs`)
	fmt.Fprintln(os.Stderr, `    create-aip: Create a new AIP`)
	fmt.Fprintln(os.Stderr, `    submit-aip: Start the submission of an AIP`)
	fmt.Fprintln(os.Stderr, `    update-aip: Signal that an AIP submission is complete`)
	fmt.Fprintln(os.Stderr, `    download-aip-request: Request access to AIP download`)
	fmt.Fprintln(os.Stderr, `    download-aip: Download AIP by AIPID`)
	fmt.Fprintln(os.Stderr, `    move-aip: Move an AIP to a permanent storage location`)
	fmt.Fprintln(os.Stderr, `    move-aip-status: Retrieve the status of a permanent storage location move of the AIP`)
	fmt.Fprintln(os.Stderr, `    reject-aip: Reject an AIP`)
	fmt.Fprintln(os.Stderr, `    show-aip: Show AIP by AIPID`)
	fmt.Fprintln(os.Stderr, `    list-aip-workflows: List workflows related to an AIP`)
	fmt.Fprintln(os.Stderr, `    list-deletion-requests: List AIP deletion requests`)
	fmt.Fprintln(os.Stderr, `    request-aip-deletion: Request an AIP deletion`)
	fmt.Fprintln(os.Stderr, `    review-aip-deletion: Review an AIP deletion request`)
	fmt.Fprintln(os.Stderr, `    cancel-aip-deletion: Cancel an AIP deletion request`)
	fmt.Fprintln(os.Stderr, `    download-deletion-report-request: Request access to download a deletion report`)
	fmt.Fprintln(os.Stderr, `    download-deletion-report: Download deletion report by UUID`)
	fmt.Fprintln(os.Stderr, `    list-locations: List locations`)
	fmt.Fprintln(os.Stderr, `    create-location: Create a storage location`)
	fmt.Fprintln(os.Stderr, `    show-location: Show location by UUID`)
	fmt.Fprintln(os.Stderr, `    list-location-aips: List all the AIPs stored in the location with UUID`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s storage COMMAND --help\n", os.Args[0])
}
func storageMonitorRequestUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage monitor-request", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request access to the /monitor WebSocket`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage monitor-request --token "abc123"`)
}

func storageMonitorUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage monitor", os.Args[0])
	fmt.Fprint(os.Stderr, " -ticket STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Obtain access to the /monitor WebSocket`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -ticket STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage monitor --ticket "abc123"`)
}

func storageListAipsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage list-aips", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -earliest-created-time STRING")
	fmt.Fprint(os.Stderr, " -latest-created-time STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprint(os.Stderr, " -offset INT")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all AIPs`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -earliest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -latest-created-time STRING: `)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)
	fmt.Fprintln(os.Stderr, `    -offset INT: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage list-aips --name "abc123" --earliest-created-time "1970-01-01T00:00:01Z" --latest-created-time "1970-01-01T00:00:01Z" --status "stored" --limit 1 --offset 1 --token "abc123"`)
}

func storageCreateAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage create-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a new AIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage create-aip --body '{
      "location_uuid": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5",
      "name": "abc123",
      "object_key": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5",
      "status": "stored",
      "uuid": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5"
   }' --token "abc123"`)
}

func storageSubmitAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage submit-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Start the submission of an AIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage submit-aip --body '{
      "name": "abc123"
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageUpdateAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage update-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Signal that an AIP submission is complete`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage update-aip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageDownloadAipRequestUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage download-aip-request", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request access to AIP download`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of the AIP to download`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage download-aip-request --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageDownloadAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage download-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -ticket STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Download AIP by AIPID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of the AIP to download`)
	fmt.Fprintln(os.Stderr, `    -ticket STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage download-aip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --ticket "abc123"`)
}

func storageMoveAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage move-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Move an AIP to a permanent storage location`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage move-aip --body '{
      "location_uuid": "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5"
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageMoveAipStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage move-aip-status", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Retrieve the status of a permanent storage location move of the AIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage move-aip-status --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageRejectAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage reject-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Reject an AIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage reject-aip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageShowAipUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage show-aip", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show AIP by AIPID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage show-aip --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageListAipWorkflowsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage list-aip-workflows", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -type STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List workflows related to an AIP`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -type STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage list-aip-workflows --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --status "in progress" --type "upload aip" --token "abc123"`)
}

func storageListDeletionRequestsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage list-deletion-requests", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List AIP deletion requests`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage list-deletion-requests --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --status "approved" --token "abc123"`)
}

func storageRequestAipDeletionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage request-aip-deletion", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request an AIP deletion`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage request-aip-deletion --body '{
      "reason": "abc123"
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageReviewAipDeletionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage review-aip-deletion", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Review an AIP deletion request`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage review-aip-deletion --body '{
      "approved": false
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageCancelAipDeletionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage cancel-aip-deletion", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Cancel an AIP deletion request`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of AIP`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage cancel-aip-deletion --body '{
      "check": false
   }' --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageDownloadDeletionReportRequestUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage download-deletion-report-request", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Request access to download a deletion report`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: UUID of the deletion report to download`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage download-deletion-report-request --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageDownloadDeletionReportUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage download-deletion-report", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -ticket STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Download deletion report by UUID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: UUID of the deletion report to download`)
	fmt.Fprintln(os.Stderr, `    -ticket STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage download-deletion-report --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --ticket "abc123"`)
}

func storageListLocationsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage list-locations", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List locations`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage list-locations --token "abc123"`)
}

func storageCreateLocationUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage create-location", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a storage location`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage create-location --body '{
      "config": {
         "Type": "s3",
         "Value": "{\"bucket\":\"abc123\",\"endpoint\":\"abc123\",\"key\":\"abc123\",\"path_style\":false,\"profile\":\"abc123\",\"region\":\"abc123\",\"secret\":\"abc123\",\"token\":\"abc123\"}"
      },
      "description": "abc123",
      "name": "abc123",
      "purpose": "aip_store",
      "source": "minio"
   }' --token "abc123"`)
}

func storageShowLocationUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage show-location", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Show location by UUID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of location`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage show-location --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}

func storageListLocationAipsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] storage list-location-aips", os.Args[0])
	fmt.Fprint(os.Stderr, " -uuid STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all the AIPs stored in the location with UUID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -uuid STRING: Identifier of location`)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `storage list-location-aips --uuid "d1845cb6-a5ea-474a-9ab8-26f9bcd919f5" --token "abc123"`)
}
